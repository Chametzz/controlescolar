<Window x:Class="controlescolar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <SolidColorBrush x:Key="MaterialDesignTextBoxUnderlineBrush" Color="#FF6F00"/>
        <SolidColorBrush x:Key="MaterialDesignTextFieldBoxActiveBorderBrush" Color="#FF6F00"/>

    </Window.Resources>
    <Grid VerticalAlignment="Center" xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf">

        <Grid.Resources>
            <Style x:Key="CustomTealTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
                <Setter Property="wpf:TextFieldAssist.UnderlineBrush" Value="Green" />
                <Setter Property="wpf:HintAssist.Foreground" Value="Green" />
            </Style>

            <Style x:Key="CustomTealPasswordBox" TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
                <Setter Property="wpf:TextFieldAssist.UnderlineBrush" Value="Green" />
                <Setter Property="wpf:HintAssist.Foreground" Value="Green" />
            </Style>
        </Grid.Resources>
        
        <StackPanel HorizontalAlignment="Center" Width="250">
            <Ellipse Width="60" Height="60" Fill="#216b4a" HorizontalAlignment="Center"/>

            <TextBox Width="200" Margin="0,0,0,10" Padding="5" x:Name="UserText" Style="{StaticResource CustomTealTextBox}" materialDesign:HintAssist.Foreground="Green"  materialDesign:HintAssist.Hint="User Name" 
                     BorderBrush="#c5c8cc" SelectionBrush="Green"/>
            <PasswordBox Width="200" Margin="0,0,0,10" Padding="5" x:Name="PasswordText" Style="{StaticResource CustomTealPasswordBox}" materialDesign:HintAssist.Foreground="Green" materialDesign:HintAssist.Hint="Password" />
            <CheckBox Content="Recordarme" Margin="0,10,0,0"/>
            <TextBlock Text="Olvide mi cotrasena" Foreground="#2196F3" HorizontalAlignment="Right" FontSize="12"/>
            <Button Content="LOGIN" Background="#216b4a" Foreground="White" Margin="0,20,0,0" Height="40" Click="Button_Click" Style="{StaticResource MaterialDesignPaperDarkButton }"/>
        </StackPanel>
    </Grid>
</Window>
